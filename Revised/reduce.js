// Use reduce() to find the sum of all numbers in an array.

// Given an array of numbers, use reduce() to find the product of all numbers.

// Use reduce() to find the maximum value in an array of numbers.

// Create an object that counts the occurrences of each element in an array using reduce().

// Use reduce() to concatenate all strings in an array into a single string.

// Given an array of objects with price properties, use reduce() to find the total price.

// Use reduce() to flatten an array of arrays into a single array.

// Given an array of arrays, use reduce() to find the sum of all elements in the nested arrays.

// Use reduce() to create an object grouping elements by a key from an array of objects.

// Use reduce() to reverse a string.