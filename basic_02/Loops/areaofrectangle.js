// // Get user input for length
// var length = parseFloat("Enter the length of the rectangle:");

// // Get user input for width
// var width = parseFloat("Enter the width of the rectangle:");

// // Check if the inputs are valid numbers
// if (isNaN(length) || isNaN(width) || length <= 0 || width <= 0) {
//     console.log();("Invalid input. Please enter valid positive numbers for length and width.");
// } else {
//     // Calculate the area
//     var area = length * width;

//     // Display the result

// console.log(area);
// }

